/*
Package webdriver provides the core W3C WebDriver client contract and transport
shared by the selenium and appium packages: the WebDriver and WebElement
interfaces, the shared value types (Capabilities, Cookie, Rect, ...) and the
remote HTTP transport, including NewRemote.

Use the selenium package to launch a local WebDriver server (a Selenium JAR,
ChromeDriver, or GeckoDriver), and the appium package to drive mobile devices
via Appium 2. Both build on the interfaces and transport defined here.

See the example below for how to get started with this API.
*/
package webdriver
